// shader* raw_shaders_delete()


// struct resource_manager
// {
//         char*           src_path;        // Path to source where resource will be checked
//         shader_program* shader_programs;
// };

// int find_shader_prog(const resource_manager* res_man, const char* shader_prog_name)
// {
//         if(res_man->shader_program == nullptr) {
//                 ErrorPrint("No shader program was added. Nothing to find\n");
//                 return -1;
//         }
//         if(shader_prog_name == nullptr) {
//                 ErropPrint("Nullptr shader program name cannot be handled");
//                 return -1;
//         }

//         hash_t shader_prog_hash = Murmurhash(shader_prog_name, strlen(shader_prog_name));

//         for(int iter_count = 0; this->shader_programs[iter_count].is_initialized != 0; iter_count++) {
//                 if()
//         }

// }
